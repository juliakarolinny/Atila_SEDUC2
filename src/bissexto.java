import static java.lang.IO.*;
void main() {

    IO.print("Digite uma ano qualquer: ");
    int ano =  Integer.parseInt(readln());

    if (ano % 4== 0 ) {
        IO.print("Ano não é bissexto!");
    } else {
       IO.print (" O ano não é bisexto!");
    }





}